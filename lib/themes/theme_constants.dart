import 'package:flutter/material.dart';

// Primary colors for both themes
const Color darkPrimaryColor = Color(0xFFff4d00);
const Color lightPrimaryColor = Color(0xFFff4d00);
const Color primaryColor = Color(0xFFff4d00); // Dark Green
const Color secondaryColor = Colors.blue; // Dark Green

// Background colors
const Color darkBackgroundColor = Color(0xFF1F1A30);
const Color lightBackgroundColor = Color(0xFFFFFFFF);

// Text field colors
const Color TextFieldColor = Colors.transparent;

// Inactive text field colors
const Color inactiveTextFieldColor = Color(0xFFCBCFD4);

// Active text field colors
const Color darkActiveTextFieldColor = Color(0xFFff4d00);
const Color lightActiveTextFieldColor = Color(0xFFff4d00);
const Color dividercolor = Color(0xFFF0F1F5);

// Define light color scheme
const lightColorScheme = ColorScheme.light(
  brightness: Brightness.light,
  primary: lightPrimaryColor,
  surface: lightBackgroundColor,
  secondary: inactiveTextFieldColor,
);

// Define dark color scheme
const darkColorScheme = ColorScheme.dark(
  brightness: Brightness.dark,
  primary: darkPrimaryColor,
  surface: darkBackgroundColor,
  secondary: inactiveTextFieldColor,
);

// Additional color constants
const blackColor = Color(0xFF000000);
const grayColor = Colors.grey;
const whiteColor = Color(0xFFFFFFFF);
const redColor = Color(0xFFD32F2F);
const greyColor = Colors.grey;
const goldColor = Color(0xFFFFD700);

// container color home screen
const lightwhite = Color(0xffFAFAFA);
const contshadow = Colors.black45;

const buttonhight = 48.0;
const p = 16.0;
const buttonradius = 8.0;
const double mainheadingsize = 24.0;
const double subheadingsize = 16.0;

const double smallspascing = 10.0;
const double lagespacing = 20.0;
const double mediumspascing = 15.0;

const double buttonpadding = 50.0;

const gogoleapi = "AIzaSyBT4nWZLp7gTLkjSTgEAZ6sCv4r_QvBAoU";

const gomap = "AlzaSypOj9d62d2URqo36NlbgawbOmh8zRinJ6O";
